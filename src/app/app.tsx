import AppHeader from './components/app-header';

const App = () => {
  const element = (
    <div className="container">
      <AppHeader />
    </div>
  );

  return element;
};

export default App;
